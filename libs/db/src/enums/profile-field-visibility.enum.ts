// libs/db/src/enums/profile-field-visibility.enum.ts

/**
 * ProfileFieldVisibility enum representing simple per-field profile visibility options.
 */
export enum ProfileFieldVisibility {
  Private = 'private',
  Public = 'public',
  ConnectionsOnly = 'connections_only',
  OrganizationOnly = 'organization_only',
}