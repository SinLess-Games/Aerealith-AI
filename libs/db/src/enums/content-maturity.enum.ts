/**
 * ContentMaturity enum representing user-selectable content maturity and safety preference levels.
 */
export enum ContentMaturity {
  Unspecified = 'unspecified',
  SafeOnly = 'safe_only',
  FamilyFriendly = 'family_friendly',
  Explicit = 'explicit',
  Restricted = 'restricted',
}