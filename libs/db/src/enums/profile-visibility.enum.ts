// libs/db/src/enums/profile-visibility.enum.ts

/**
 * ProfileVisibility enum representing simple whole-profile visibility options.
 */
export enum ProfileVisibility {
  Private = 'private',
  Public = 'public',
  Unlisted = 'unlisted',
  ConnectionsOnly = 'connections_only',
  OrganizationOnly = 'organization_only',
}